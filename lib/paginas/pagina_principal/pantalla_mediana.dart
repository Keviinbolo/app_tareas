import 'package:flutter/material.dart';

class PantallaMediana extends StatelessWidget {
  const PantallaMediana({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Pantalla Mediana')),
    );
  }
}